[package]
name = "rustlang"
version = "0.1.0"
edition = "2021"

[dependencies]
inkwell = { version = "0.5", features = ["llvm18-0"] }
libc = "0.2"
libloading = "0.8"
object = "0.36"           # For parsing ELF files and stack maps
memmap2 = "0.9"           # For memory-mapped files
